# Canonical Context Records

This directory holds normalized, source-backed context records.

Each record must include:

- `## Source Basis`
- `## Fact vs Inference`
- `## Artifact Inventory`
- `## Open Conflicts`

Records that do not satisfy those sections fail `npm run context:lint`.

