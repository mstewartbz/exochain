{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "./Dockerfile"
  },
  "deploy": {
    "_comment_startCommand": "Empty — Dockerfile CMD invokes /app/entrypoint.sh which reads env vars (IS_VALIDATOR, SEED_ADDR, EXOCHAIN_DATA_DIR, PORT, P2P_PORT, VALIDATORS).",
    "healthcheckPath": "/ready",
    "_comment_healthcheckTimeout": "Per-probe timeout in seconds. /ready verifies gateway database readiness so dependency failures stop rollout.",
    "healthcheckTimeout": 15,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
